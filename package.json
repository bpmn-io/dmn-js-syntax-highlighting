{
  "name": "root",
  "private": true,
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-plugin-bpmn-io": "^0.11.0",
    "lerna": "^3.20.2",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build": "lerna run build",
    "lerna-start": "lerna run start --scope dmn-js-editor-demo",
    "lerna-dev": "lerna run dev --scope dmn-js-editor-demo",
    "dev": "run-s lerna-dev",
    "start": "run-s lerna-start",
    "postinstall": "lerna bootstrap",
    "lint": "eslint ."
  }
}
